#!/usr/bin/python3
__import__("os").write(1, "#pythoniscool\n".encode("UTF-8"))

# Importing the os module.
# Calling the write() function on the os module.
# Passing the file descriptor 1 and the string #pythoniscool\n encoded
# in UTF-8 to the write() function.
# the encode function encodes a string to a byte string in UTF-8 encoding
