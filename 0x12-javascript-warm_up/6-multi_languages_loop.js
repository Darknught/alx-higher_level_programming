#!/usr/bin/node
// A Script that prints 3 lines with newline char but by using array and loop
const myArray = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (const item of myArray) {
  console.log(item);
}
